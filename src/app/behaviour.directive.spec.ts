import { BehaviourDirective } from './behaviour.directive';

describe('BehaviourDirective', () => {
  it('should create an instance', () => {
    const directive = new BehaviourDirective();
    expect(directive).toBeTruthy();
  });
});
